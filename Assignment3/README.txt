CLient side validation:
->when using HTML 5 to submit a form, it is possible for the user to go to the inspect page and change the forms data rem oving the required field from each type entirely, thbus bypassing the signup form
->Using client side verification ensures that the data must pass the regex fields for it to be able to be sent to the server for verification and storage meaning that the user has to enter valid data according to the REGEX strings for it to be accpepted  

